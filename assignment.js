/*1.let ageCanDrive = 30
if (ageCanDrive >= 18 ){
      console.log( 'You ae old enough to drive')
} else { let yearsLeft= 18- ageCanDrive ;
      console.log(`you are left with` + yearsLeft + `years to drive`)
}

let enterYourAge  = 15
if (enterYourAge >= 18 ){
      console.log( 'You ae old enough to drive')
} else { let yearsLeft = 18 - enterYourAge;
      console.log(`you are left with` + ` `+ yearsLeft + ` ` + `years to drive`)
}

2. let myAge  = 25;
let yourAge = 30;
if ( myAge > yourAge ){
      console.log( 'You ae older')
} else { let ageDifference = yourAge - myAge;
      console.log(`You are` + ` `+ ageDifference  + ` ` + `years older than me`)
}

3. //using if/else
let a = 4;
let b = 3;
if (a > b){
     console.log( ` a is greater than  b`)
}else {
    console.log( `a is less than  b`)
}


//using tenary operator
let A = 4;
let B = 3;
let result =  (A > B) ? `a is greater than b` : `a is less tham b`;
 console.log(result)
*/

4. 
const evenOdd = (x);
if (x % 2 === 0) {
     return (x + `is an even number`);
}else {
    return ( x `is an odd number`)};
    console.log(evenOdd());
     console.log(evenOdd())